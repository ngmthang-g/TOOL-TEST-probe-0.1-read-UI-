name: build-windows

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: windows-latest
    steps:
      - run: echo "Automatic build temporarily paused while v0.1.2 source is updated"
